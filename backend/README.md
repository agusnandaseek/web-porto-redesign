# Backend Service - Nanda Creative Portfolio

Direktori ini disediakan untuk service backend (API / Database Supabase / Contact Form Handler / Auth) di masa mendatang.

## Rencana Struktur Backend (Future Scope):
- `server.js` / Express API or Supabase Edge Functions
- `/routes` - Contact form submission endpoint, portfolio data CRUD
- `/config` - Database and environment config
